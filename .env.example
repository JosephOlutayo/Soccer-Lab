# Copy to .env.local and fill in. Get a free key at
# https://www.football-data.org/client/register
#
# No NEXT_PUBLIC_ prefix — that is deliberate. This value must stay on the
# server; anything prefixed NEXT_PUBLIC_ is inlined into the browser bundle.
FOOTBALL_DATA_API_KEY=
